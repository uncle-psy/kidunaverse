import { layoutTypes } from './data.js?v=0.9.0';
import { icon } from './icons.js';

const escapeHtml = (value = '') => String(value).replace(/[&<>'"]/g, character => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;' }[character]));

export const expressions = [
  { id: 'welcome-path', title: 'A Place to Contribute', eyebrow: 'Service Alliance · invitation', description: 'Meet the people, see the work already moving, and find a way to help that fits.', layout: 'composed', realm: 'Service Alliance', source: 'Alice', privacy: 'Private', access: 'Invitation-scoped', recent: 7, saved: true, parts: ['Service Alliance Realm', 'Kinship Map', 'Invitation Seed', 'Alice Transmitter'], image: 'assets/sigils/invitation.png', why: 'Alice invited you, and two open needs match your interests.' },
  { id: 'build-room', title: 'Build Kiduna Working Room', eyebrow: 'Your team’s room', description: 'Current releases, open decisions, contributors, and tools—together in one place.', layout: 'setting', realm: 'Build Kiduna', source: 'Moto', privacy: 'Private', access: 'Realm members', recent: 6, saved: true, parts: ['Build Kiduna Realm', 'Contribution Map', 'Layout Seed'], image: 'assets/sigils/alliance.png', why: 'You have active work here and two decisions waiting.' },
  { id: 'signal-weather', title: 'What’s Gathering Near You', eyebrow: 'A living view of what matters now', description: 'Invitations, care needs, decisions, and available support moving around your work.', layout: 'pulse', realm: 'Service Alliance', source: 'Ki', privacy: 'Private', access: 'Tailored to you', recent: 5, saved: false, parts: ['Care Threshold Node', 'Invitation Signals', 'Receiver profile'], why: 'Three new things connect directly to the welcome work you follow.' },
  { id: 'systems-oracle', title: 'Systems Oracle', eyebrow: 'Follow the forces shaping technology', description: 'Explore the institutions, incentives, and relationships behind the tools changing our lives.', layout: 'graph', realm: 'Knowledge & Frontier', source: 'Mapshifting', privacy: 'Public', access: 'Open', recent: 4, saved: true, parts: ['Systems Oracle Map', 'Authority Nodes', 'Research Seed'], image: 'assets/sigils/actions.png', why: 'It connects to your recent research on human agency and AI.' },
  { id: 'signals-film', title: 'How Signals Move', eyebrow: 'A 7-minute visual story', description: 'See how something travels across the network, reaches the right place, and becomes useful.', layout: 'theater', realm: 'Kiduna Commons', source: 'Moto', privacy: 'Public', access: 'Open', recent: 3, saved: false, parts: ['Transceiver Media', 'Landing diagrams', 'Public Seed'], why: 'A clear introduction to the system you’ve been shaping.' },
  { id: 'future-commons', title: 'The Future Commons', eyebrow: 'Walk through a possible future', description: 'Explore gathering spaces, shared resources, civic tools, and new ways to act together.', layout: 'world', realm: 'Kiduna Commons', source: 'Commons Studio', privacy: 'Public', access: 'Rehearsal', recent: 2, saved: false, parts: ['Commons Realm', 'Civic Map', 'World Seed'], why: 'A guided walkthrough is open this week.' },
  { id: 'field-notebook', title: 'Invitation as Orientation', eyebrow: 'Field Notes · 8 minute read', description: 'A thoughtful essay on inviting people into living work without asking for blind commitment.', layout: 'reader', realm: 'Moto’s Field', source: 'Moto', privacy: 'Personal', access: 'Source only', recent: 1, saved: true, parts: ['Field Notebook Seed', 'Kinship Map', 'Audio notes'], why: 'You saved this for a quieter moment.' },
  { id: 'royals-table', title: 'Royals & Rogues', eyebrow: 'A strategy game with friends', description: 'Choose a house, make alliances, take risks, and see who can hold the realm together.', layout: 'game', realm: 'Culture & Play', source: 'Moto', privacy: 'Private', access: 'Rehearsal participants', recent: 8, saved: false, parts: ['Royals & Rogues Seed', 'Rules Map', 'Player Signals'], why: 'A table is forming now; three friends are ready to play.' }
];

const purposeScopes = {
  network: { name: 'Across Kiduna', purpose: 'People turning shared purpose into useful work, durable livelihoods, and lasting change.', people: '18,420', contributors: '6,280', volume: '$12.8M', treasury: '$3.4M', payments: '$4.6M', growth: '+18%', color: '#a876ff' },
  build: { name: 'Tools People Can Trust', purpose: 'Build technology that expands human agency and makes power easier to see, question, and share.', people: '2,840', contributors: '1,120', volume: '$3.1M', treasury: '$820K', payments: '$1.4M', growth: '+22%', color: '#68d8ff' },
  care: { name: 'Care Within Reach', purpose: 'Make practical care easier to find, offer, and sustain wherever people already live and gather.', people: '4,620', contributors: '1,760', volume: '$2.7M', treasury: '$690K', payments: '$980K', growth: '+27%', color: '#ff7aa8' },
  watersheds: { name: 'Living Watersheds', purpose: 'Restore local waters and the communities, knowledge, and livelihoods that depend on them.', people: '3,180', contributors: '980', volume: '$1.9M', treasury: '$540K', payments: '$620K', growth: '+14%', color: '#56d7ae' },
  creators: { name: 'Independent Creators', purpose: 'Help creators make meaningful work, reach the right people, and build durable livelihoods.', people: '5,240', contributors: '2,010', volume: '$3.6M', treasury: '$760K', payments: '$1.6M', growth: '+19%', color: '#ffc95c' }
};

const workspaceMeta = {
  connector: ['Connector', 'Keep the people, places, and tools you rely on close—and stay in control of every connection.'],
  creator: ['Creator', 'Turn an idea into something people can explore, use, or join.'],
  'inspector-workspace': ['Inspector', 'See where something came from, what changed, and who did what.'],
  broker: ['Broker', 'See what you have, what is moving, and where work is creating value.'],
  envoy: ['Envoy', 'Vote directly—or set clear limits for decisions you trust to others.'],
  sentinel: ['Sentinel', 'Choose what reaches you, how it arrives, and what needs a warning.'],
  metrics: ['Metrics', 'See where people are showing up, how value is moving, and what the work is making possible.']
};

function workspaceShell(id, eyebrow, content, tabs = '') {
  const [title, description] = workspaceMeta[id];
  const stableAnchor = id === 'metrics' ? ' id="network-overview"' : '';
  return `<section class="tool-workspace tool-${id}"${stableAnchor} data-open-panel="${id}" aria-label="${title}"><div class="tool-atmosphere" aria-hidden="true"></div><header class="tool-workspace-header"><div><span>${escapeHtml(eyebrow)}</span><h1>${title}</h1><p>${escapeHtml(description)}</p></div><button class="tool-close" type="button" data-close-panel aria-label="Close ${title}">${icon('close', 20)}</button></header>${tabs}<div class="tool-workspace-scroll">${content}</div></section>`;
}

function legacyFieldExplorer(state) {
  const query = state.expressionQuery.toLowerCase();
  let items = expressions.filter(item => (!query || Object.values(item).flat().join(' ').toLowerCase().includes(query)) && (state.expressionLayout === 'All' || item.layout === state.expressionLayout) && (state.expressionRealm === 'All' || item.realm === state.expressionRealm) && (state.expressionSource === 'All' || item.source === state.expressionSource) && (state.expressionSeed === 'All' || item.parts.some(part => part === state.expressionSeed)) && (!state.expressionSavedOnly || item.saved));
  items.sort((a, b) => state.expressionSort === 'Oldest' ? a.recent - b.recent : state.expressionSort === 'Title' ? a.title.localeCompare(b.title) : b.recent - a.recent);
  const realms = ['All', ...new Set(expressions.map(item => item.realm))];
  const sources = ['All', ...new Set(expressions.map(item => item.source))];
  const seeds = ['All', ...new Set(expressions.flatMap(item => item.parts.filter(part => part.endsWith('Seed'))))];
  return `<section class="expression-explorer" aria-label="Expression explorer"><div class="expression-atmosphere" aria-hidden="true"></div><header><div><span>Field Canvas · proposed Layout Kit vocabulary</span><h1>Explore Expressions</h1><p>A complete piece present to you—composed from authorized Realms, Maps, Transmitters, Media, Nodes, and Seeds.</p></div><button class="expression-create" type="button" data-panel="creator">Create an Expression</button></header><section class="expression-controls"><label>${icon('search', 17)}<span class="sr-only">Search Expressions</span><input id="expression-search" type="search" value="${escapeHtml(state.expressionQuery)}" placeholder="Search Expressions, Realms, Maps, Transmitters, or Seeds…"></label><select id="expression-layout" aria-label="Filter by Layout"><option>All</option>${layoutTypes.map(type => `<option value="${type.id}" ${state.expressionLayout === type.id ? 'selected' : ''}>${type.name}</option>`).join('')}</select><select id="expression-realm" aria-label="Filter by Realm">${realms.map(realm => `<option ${state.expressionRealm === realm ? 'selected' : ''}>${escapeHtml(realm)}</option>`).join('')}</select><select id="expression-sort" aria-label="Sort Expressions"><option ${state.expressionSort === 'Most recent' ? 'selected' : ''}>Most recent</option><option ${state.expressionSort === 'Oldest' ? 'selected' : ''}>Oldest</option><option ${state.expressionSort === 'Title' ? 'selected' : ''}>Title</option></select><button class="${state.expressionSavedOnly ? 'active' : ''}" type="button" data-action="expression-saved">★ Saved</button></section><div class="expression-grid">${items.map(item => `<article class="expression-card layout-${item.layout}"><div class="expression-art">${item.image ? `<img src="${item.image}" alt="">` : `<span>${item.layout.slice(0,2)}</span>`}<i>${layoutTypes.find(type => type.id === item.layout)?.name}</i></div><div class="expression-copy"><span>${escapeHtml(item.eyebrow)}</span><h2>${escapeHtml(item.title)}</h2><p>${escapeHtml(item.description)}</p><dl><div><dt>Realm</dt><dd>${escapeHtml(item.realm)}</dd></div><div><dt>Source</dt><dd>${escapeHtml(item.source)}</dd></div><div><dt>Boundary</dt><dd>${escapeHtml(item.privacy)} · ${escapeHtml(item.access)}</dd></div></dl><small><b>Includes:</b> ${item.parts.map(escapeHtml).join(' · ')}</small><small><b>Why here:</b> ${escapeHtml(item.why)}</small></div><footer><button type="button" data-expression-favorite="${item.id}" aria-pressed="${item.saved}">${item.saved ? '★ Saved' : '☆ Save'}</button><button type="button" data-expression-inspect="${item.id}">Inspect</button><button class="primary" type="button" data-expression-open="${item.id}">Open Expression</button></footer></article>`).join('') || '<div class="tool-empty"><h2>No visible Expressions</h2><p>Adjust search or filters. Hidden material is not disclosed.</p></div>'}</div></section>`;
}

function previousFieldExplorer(state) {
  const query = state.expressionQuery.toLowerCase();
  const realms = ['All', ...new Set(expressions.map(item => item.realm))];
  const sources = ['All', ...new Set(expressions.map(item => item.source))];
  const seeds = ['All', ...new Set(expressions.flatMap(item => item.parts.filter(part => part.includes('Seed'))))];
  let items = expressions.filter(item => {
    const searchable = Object.values(item).flat().join(' ').toLowerCase();
    return (!query || searchable.includes(query))
      && (state.expressionLayout === 'All' || item.layout === state.expressionLayout)
      && (state.expressionRealm === 'All' || item.realm === state.expressionRealm)
      && (state.expressionSource === 'All' || item.source === state.expressionSource)
      && (state.expressionSeed === 'All' || item.parts.includes(state.expressionSeed))
      && (!state.expressionSavedOnly || item.saved);
  });
  items.sort((a, b) => state.expressionSort === 'Oldest' ? a.recent - b.recent : state.expressionSort === 'Title' ? a.title.localeCompare(b.title) : b.recent - a.recent);
  const options = (values, selected) => values.map(value => `<option ${selected === value ? 'selected' : ''}>${escapeHtml(value)}</option>`).join('');
  return `<section class="expression-explorer" aria-label="Expression explorer"><div class="expression-atmosphere" aria-hidden="true"></div><header><div><span>Field Canvas · proposed Layout Kit vocabulary</span><h1>Explore Expressions</h1><p>A complete piece present to you—composed from authorized Realms, Maps, Transmitters, Media, Nodes, and Seeds.</p></div><button class="expression-create" type="button" data-panel="creator">Create an Expression</button></header><section class="expression-controls"><label>${icon('search',17)}<span class="sr-only">Search Expressions</span><input id="expression-search" type="search" value="${escapeHtml(state.expressionQuery)}" placeholder="Search Expressions, Realms, Maps, Transmitters, or Seeds…"></label><select id="expression-layout" aria-label="Filter by Layout"><option>All</option>${layoutTypes.map(type => `<option value="${type.id}" ${state.expressionLayout === type.id ? 'selected' : ''}>${type.name}</option>`).join('')}</select><select id="expression-realm" aria-label="Filter by Realm">${options(realms,state.expressionRealm)}</select><select id="expression-source" aria-label="Filter by Source or Transmitter">${options(sources,state.expressionSource)}</select><select id="expression-seed" aria-label="Filter by Seed">${options(seeds,state.expressionSeed)}</select><select id="expression-sort" aria-label="Sort Expressions">${options(['Most recent','Oldest','Title'],state.expressionSort)}</select><button class="${state.expressionSavedOnly ? 'active' : ''}" type="button" data-action="expression-saved">★ Saved</button></section><div class="expression-grid">${items.map(item => `<article class="expression-card layout-${item.layout}"><div class="expression-art">${item.image ? `<img src="${item.image}" alt="">` : `<span>${item.layout.slice(0,2)}</span>`}<i>${layoutTypes.find(type => type.id === item.layout)?.name}</i></div><div class="expression-copy"><span>${escapeHtml(item.eyebrow)}</span><h2>${escapeHtml(item.title)}</h2><p>${escapeHtml(item.description)}</p><dl><div><dt>Realm</dt><dd>${escapeHtml(item.realm)}</dd></div><div><dt>Source</dt><dd>${escapeHtml(item.source)}</dd></div><div><dt>Boundary</dt><dd>${escapeHtml(item.privacy)} · ${escapeHtml(item.access)}</dd></div></dl><small><b>Includes:</b> ${item.parts.map(escapeHtml).join(' · ')}</small><small><b>Why here:</b> ${escapeHtml(item.why)}</small></div><footer><button type="button" data-expression-favorite="${item.id}" aria-pressed="${item.saved}">${item.saved ? '★ Saved' : '☆ Save'}</button><button type="button" data-expression-inspect="${item.id}">Inspect</button><button class="primary" type="button" data-expression-open="${item.id}">Open Expression</button></footer></article>`).join('') || '<div class="tool-empty"><h2>No visible Expressions</h2><p>Adjust search or filters. Hidden material is not disclosed.</p></div>'}</div></section>`;
}

export function fieldExplorer(state) {
  const query = state.expressionQuery.toLowerCase();
  const activePurpose = purposeScopes[state.metricsScope] || purposeScopes.network;
  const realms = ['All', ...new Set(expressions.map(item => item.realm))];
  const sources = ['All', ...new Set(expressions.map(item => item.source))];
  let items = expressions.filter(item => {
    const searchable = Object.values(item).flat().join(' ').toLowerCase();
    return (!query || searchable.includes(query))
      && (state.expressionLayout === 'All' || item.layout === state.expressionLayout)
      && (state.expressionRealm === 'All' || item.realm === state.expressionRealm)
      && (state.expressionSource === 'All' || item.source === state.expressionSource)
      && (!state.expressionSavedOnly || item.saved);
  });
  items.sort((a, b) => state.expressionSort === 'Oldest' ? a.recent - b.recent : state.expressionSort === 'Title' ? a.title.localeCompare(b.title) : b.recent - a.recent);
  const layoutOptions = layoutTypes.map(type => `<option value="${type.id}" ${state.expressionLayout === type.id ? 'selected' : ''}>${type.name}</option>`).join('');
  const namedOptions = (values, selected, allLabel) => values.map(value => `<option value="${escapeHtml(value)}" ${selected === value ? 'selected' : ''}>${value === 'All' ? allLabel : escapeHtml(value)}</option>`).join('');
  const purposeOptions = Object.entries(purposeScopes).map(([id, scope]) => `<option value="${id}" ${id === state.metricsScope ? 'selected' : ''}>${escapeHtml(scope.name)}</option>`).join('');
  return `<main class="field-home" aria-label="Field">
    <div class="field-atmosphere" aria-hidden="true"></div>
    <section class="field-hero">
      <div class="field-welcome"><span>Your world, in motion</span><h1>Find what moves you.</h1><p>Explore places, stories, tools, and games shaped by people working on what matters.</p></div>
      <div class="network-glance" aria-label="Kiduna at a glance">
        <span class="prototype-note">Prototype network view</span>
        <article><strong>18,420</strong><small>people taking part</small></article>
        <article><strong>$12.8M</strong><small>value moving</small></article>
        <article><strong>$4.6M</strong><small>paid to contributors</small></article>
        <article><strong>$3.4M</strong><small>shared resources</small></article>
      </div>
    </section>
    <section class="purpose-spotlight" style="--purpose-color:${activePurpose.color}">
      <div class="purpose-story"><span>Purpose in motion</span><label for="metrics-scope">See what is happening in</label><select id="metrics-scope">${purposeOptions}</select><h2>${escapeHtml(activePurpose.purpose)}</h2><p>Follow the people, resources, and real work gathering around this purpose.</p></div>
      <div class="purpose-stats">
        <article><strong>${activePurpose.people}</strong><small>people</small></article>
        <article><strong>${activePurpose.contributors}</strong><small>active contributors</small></article>
        <article><strong>${activePurpose.payments}</strong><small>paid for work</small></article>
        <article><strong>${activePurpose.growth}</strong><small>participation growth</small></article>
      </div>
      <div class="purpose-actions"><button class="primary" type="button" data-panel="metrics">See the whole picture</button><button type="button" data-prototype-action="Find a way to help ${escapeHtml(activePurpose.name)}">Find your place</button></div>
    </section>
    <section class="field-discovery">
      <header class="discovery-heading"><div><span>For you, right now</span><h2>Places to go. Things to try. Work worth joining.</h2></div><p>${items.length} ${items.length === 1 ? 'result' : 'results'}</p></header>
      <div class="discovery-controls">
        <label class="discovery-search">${icon('search',17)}<span class="sr-only">Search the Field</span><input id="expression-search" type="search" value="${escapeHtml(state.expressionQuery)}" placeholder="Search places, stories, tools, and games…"></label>
        <select id="expression-layout" aria-label="Filter by format"><option value="All">Every format</option>${layoutOptions}</select>
        <select id="expression-realm" aria-label="Filter by place">${namedOptions(realms,state.expressionRealm,'Every place')}</select>
        <select id="expression-source" aria-label="Filter by source">${namedOptions(sources,state.expressionSource,'Every source')}</select>
        <select id="expression-sort" aria-label="Sort results"><option ${state.expressionSort === 'Most recent' ? 'selected' : ''}>Most recent</option><option ${state.expressionSort === 'Oldest' ? 'selected' : ''}>Oldest</option><option ${state.expressionSort === 'Title' ? 'selected' : ''}>Title</option></select>
        <button class="saved-filter ${state.expressionSavedOnly ? 'active' : ''}" type="button" data-action="expression-saved">${state.expressionSavedOnly ? '★ Saved only' : '☆ Show saved'}</button>
      </div>
      <div class="experience-grid">${items.map(item => {
        const type = layoutTypes.find(layout => layout.id === item.layout);
        return `<article class="experience-card layout-${item.layout}">
          <div class="experience-visual">${item.image ? `<img src="${item.image}" alt="">` : `<span>${item.layout.slice(0,2)}</span>`}<i>${type?.name || item.layout}</i><button type="button" data-expression-favorite="${item.id}" aria-label="${item.saved ? 'Remove from saved' : 'Save'} ${escapeHtml(item.title)}" aria-pressed="${item.saved}">${item.saved ? '★' : '☆'}</button></div>
          <div class="experience-body"><span>${escapeHtml(item.eyebrow)}</span><h3>${escapeHtml(item.title)}</h3><p>${escapeHtml(item.description)}</p><div class="experience-meta"><em>${escapeHtml(item.realm)}</em><em>By ${escapeHtml(item.source)}</em><em>${escapeHtml(item.access)}</em></div><small><b>Why this?</b> ${escapeHtml(item.why)}</small></div>
          <footer><button type="button" data-expression-inspect="${item.id}">Details</button><button class="primary" type="button" data-expression-open="${item.id}">Open</button></footer>
        </article>`;
      }).join('') || '<div class="tool-empty"><h2>Nothing here yet</h2><p>Try a broader search or clear one of the filters.</p></div>'}</div>
    </section>
  </main>`;
}

function connectorWorkspace(state) {
  const tab = state.connectorWorkspaceTab;
  const agents = [
    ['moto-avatar','Moto · Build Kiduna','Avatar','Represents Moto in Build Kiduna','Catalyst Role; representation only'],
    ['research-ally','Research Ally','Ally','Personal companion relationship','Draft and advise; revocable'],
    ['budget-actor','Budget Actor','Actor','Build Kiduna Realm operator','Receipts within weekly threshold'],
    ['alice-avatar','Alice','Avatar','Direct Source relationship','Visible relationship context only']
  ];
  const systems = [
    ['google','Google','Gmail · Meet · Calendar · YouTube'],['bluesky','Bluesky','Public social publishing'],['telegram','Telegram','Messaging and groups'],['solana','Solana','Wallet and settlement rail']
  ];
  const realms = [
    ['build','Build Kiduna','Member · Catalyst','Manage Roles'],['service','Service Alliance','Guest · invited','Request new Role'],['commons','Kiduna Commons','Not joined','Join']
  ];
  const connection = id => state.connectorConnections[id] || { connected: false, active: false, muted: false };
  let content = '';
  if (tab === 'Agents') content = `<div class="tool-card-grid">${agents.map(([id,name,type,relationship,authority]) => { const status = connection(id); return `<article class="tool-card"><span>${type} · ${status.connected ? 'Connected' : 'Disconnected'} · ${status.active ? 'Active' : 'Inactive'}</span><h2>${name}</h2><p>${relationship}</p><small>${authority}</small><div class="tool-actions"><button data-connection-action="${status.connected ? 'disconnect' : 'connect'}" data-connection-id="${id}">${status.connected ? 'Disconnect' : 'Connect'}</button><button data-connection-action="${status.active ? 'deactivate' : 'activate'}" data-connection-id="${id}" ${status.connected ? '' : 'disabled'}>${status.active ? 'Deactivate' : 'Activate'}</button><button data-connection-action="mute" data-connection-id="${id}">${status.muted ? 'Unmute' : 'Mute'}</button><button data-prototype-action="Chat with ${name}">Chat</button><button data-prototype-action="Inspect ${name}">Inspect</button></div></article>`; }).join('')}</div>`;
  if (tab === 'Systems') content = `<div class="tool-card-grid">${systems.map(([id,name,services]) => { const status = connection(id); return `<article class="tool-card"><span>System · ${status.connected ? 'Prototype connected' : 'Not connected'} · ${status.active ? 'Active' : 'Inactive'}</span><h2>${name}</h2><p>${services}</p><small>Last synchronization: ${status.connected ? 'Prototype state only · no live sync' : 'Never'}</small><div class="tool-actions"><button data-connection-action="${status.connected ? 'disconnect' : 'connect'}" data-connection-id="${id}">${status.connected ? 'Disconnect' : 'Connect'}</button><button data-connection-action="${status.active ? 'deactivate' : 'activate'}" data-connection-id="${id}" ${status.connected ? '' : 'disabled'}>${status.active ? 'Deactivate' : 'Activate'}</button><button data-prototype-action="Configure ${name}">Configure</button><button data-prototype-action="Review ${name} permissions">Permissions</button><button data-prototype-action="Inspect ${name}">Inspect</button></div></article>`; }).join('')}</div>`;
  if (tab === 'Realms') content = `<div class="tool-card-grid">${realms.map(([id,name,role,next]) => `<article class="tool-card"><span>Realm relationship</span><h2>${name}</h2><p>${role}</p><small>Joining, invitation, Role, Consent, and Authority remain distinct.</small><div class="tool-actions"><button data-prototype-action="${next} in ${name}">${next}</button>${id !== 'commons' ? `<button data-prototype-action="Leave ${name}">Leave</button>` : ''}<button data-prototype-action="Invite to ${name}">Invite</button><button data-prototype-action="Inspect ${name}">Inspect</button></div></article>`).join('')}<article class="tool-card invitation-codes"><span>Invitation codes</span><h2>Welcome paths</h2><p>Realm or Kiduna destination · Host context · optional Sponsorship, Coupon, or Gift.</p><small>Expiry, acceptance limit, remaining uses, audience, access proposal, revocation, and history remain inspectable.</small><div class="tool-actions"><button data-prototype-action="Create invitation code">Create code</button><button data-prototype-action="Manage invitation codes">Manage codes</button></div></article></div>`;
  const tabs = `<nav class="tool-tabs" aria-label="Connector sections">${['Agents','Systems','Realms'].map(item => `<button class="${tab === item ? 'active' : ''}" type="button" data-tool-tab="connectorWorkspaceTab" data-tool-tab-value="${item}">${item}</button>`).join('')}</nav>`;
  return workspaceShell('connector', 'Connections are scoped, inspectable, and revocable', content, tabs);
}

function creatorWorkspace(state) {
  const groups = [['Meaning',['Maps','Realms','Nodes','Media']],['Agents',['Avatars','Allies','Actors','Automations']],['Exchange',['Offers','Resources','Actions','DUNAs']],['Expressions',['Composed','Setting','Pulse','Graph','Theater','World','Reader','Game']]];
  const layouts = { composed:'Connected interface, Overlays, Panels, and Chrome.', setting:'Isometric room with depth and reusable spatial grammar.', pulse:'Layers, scatter, and viewport for many things in relation.', graph:'Cards and boxes joined by typed relationships.', theater:'Focused stage for watching and listening.', world:'Full 3D environment for embodied presence and movement.', reader:'Editorial flow for documents, books, magazines, audio, and video.', game:'Playable rules, choices, feedback, challenge, and shared or solo state.' };
  return workspaceShell('creator', 'Studio · proposed Layout Kit creation flow', `<div class="creator-groups">${groups.map(([group,items]) => `<section><span>${group}</span><div>${items.map(item => `<button type="button" data-create-kind="${item}">${item}</button>`).join('')}</div></section>`).join('')}</div><section class="creator-flow"><div><span>Expression draft</span><h2>Compose a complete piece</h2><p>Preview, Rehearsal, publication, and Live availability remain separate.</p></div><div class="creator-fields"><label>Name<input id="creator-name" value="${escapeHtml(state.creatorDraftName)}" placeholder="Untitled Expression"></label><label>Purpose<textarea id="creator-purpose" rows="3" placeholder="What should this Expression make possible?">${escapeHtml(state.creatorDraftPurpose)}</textarea></label><label>Privacy<select><option>Private</option><option>Public</option><option>Secret</option><option>Personal</option></select></label><label>Contributors<input placeholder="Moto, Allies, Realm contributors…"></label></div><div class="layout-example-grid">${layoutTypes.map(type => `<button class="layout-example ${state.creatorLayout === type.id ? 'selected' : ''}" type="button" data-creator-layout="${type.id}"><span>${type.name}</span><b>${escapeHtml(type.meta)}</b><p>${escapeHtml(layouts[type.id])}</p></button>`).join('')}</div><div class="creator-stage-actions"><button data-prototype-action="Preview Expression draft">Preview Draft</button><button data-prototype-action="Move Expression draft to Rehearsal">Prepare Rehearsal</button><button data-prototype-action="Review Expression publication">Publication review</button></div></section>`);
}

function inspectorWorkspace(state) {
  const records = [
    ['Signal','Alice invitation received','Alice → registered Receptor','Delivered to private Locus; not accepted or acted upon'],
    ['Action','Layout Site deployed','Moto · explicit instruction','Execution receipt preserved · production alias updated'],
    ['Record','Compute allocation receipt','Budget Actor','Verified provider receipt · Secret boundary'],
    ['Expression','Systems Oracle opened','Moto','Graph presentation; no new truth or Authority established'],
    ['Correction','Realm Role label corrected','Build Kiduna Governance','Prior Record superseded; history retained']
  ];
  const query = state.inspectorWorkspaceQuery.toLowerCase();
  return workspaceShell('inspector-workspace', 'Evidence before inference', `<section class="tool-filterbar"><label>${icon('search',16)}<input id="inspector-workspace-search" value="${escapeHtml(state.inspectorWorkspaceQuery)}" placeholder="Search objects, history, Sources, Realms, receipts…"></label><select><option>All object types</option><option>Signals</option><option>Actions</option><option>Records</option><option>Expressions</option></select><select><option>All Realms</option><option>Build Kiduna</option><option>Service Alliance</option></select><select><option>Any time</option><option>Today</option><option>Last 7 days</option><option>Custom range</option></select></section><div class="inspector-ledger">${records.filter(row => !query || row.join(' ').toLowerCase().includes(query)).map(([type,title,provenance,detail]) => `<article><span>${type}</span><h2>${title}</h2><p>${detail}</p><small><b>Provenance:</b> ${provenance}</small><div><button data-prototype-action="Inspect history for ${title}">History</button><button data-prototype-action="Inspect provenance for ${title}">Provenance</button><button data-prototype-action="Inspect boundaries for ${title}">Authority &amp; privacy</button></div></article>`).join('')}</div><aside class="distinction-strip"><b>Keep distinct</b><span>Object ≠ history</span><span>Proposal ≠ Action</span><span>Action ≠ receipt</span><span>Transmission ≠ delivery</span><span>Delivery Landing ≠ Semantic Landing</span><span>Visible ≠ verified</span></aside>`);
}

function brokerWorkspace(state) {
  const tab = state.brokerTab;
  const content = tab === 'Sponsorships' ? `<div class="tool-card-grid"><article class="tool-card feature"><span>Sponsorship · proposal</span><h2>Support the Welcome Path</h2><p>500 compute from Moto to Service Alliance for three invitation rehearsals.</p><small>Private · disclosure required · no promotional control · Realm approval pending</small><div class="tool-actions"><button data-prototype-action="Inspect Welcome Path sponsorship">Inspect terms</button><button data-prototype-action="Review Welcome Path sponsorship">Review proposal</button></div></article><article class="tool-card"><span>Gift · no reciprocal requirement</span><h2>Research Capacity Gift</h2><p>100 compute to Research Ally for evidence gathering.</p><small>Personal boundary · revocable before acceptance</small><div class="tool-actions"><button data-prototype-action="Inspect Research Capacity Gift">Inspect</button></div></article></div>` : `<div class="broker-metrics"><article><span>Personal Vault</span><strong>31,840</strong><small>Compute capacity</small></article><article><span>Build treasury</span><strong>4,820</strong><small>Themecoins</small></article><article><span>Pending approvals</span><strong>2</strong><small>No Resources moved</small></article><article><span>Receipts</span><strong>18</strong><small>1 failed · inspectable</small></article></div><div class="tool-card-grid"><article class="tool-card"><span>Offer</span><h2>Design review exchange</h2><p>Review two interface studies for 240 compute.</p><small>Draft Offer · no Commitment yet</small></article><article class="tool-card"><span>Realm treasury</span><h2>Service Alliance</h2><p>$7.2k held · 1 active allocation · 2 proposed Gifts.</p><small>Realm-governed view</small></article><article class="tool-card"><span>Resources</span><h2>Coupons, Gifts &amp; Rewards</h2><p>3 Coupons · 2 Gifts · 4 unclaimed Rewards.</p><small>Access and transferability vary</small></article></div>`;
  const tabs = `<nav class="tool-tabs" aria-label="Broker sections">${['Overview','Sponsorships','Offers','Treasuries','History'].map(item => `<button class="${tab === item ? 'active' : ''}" data-tool-tab="brokerTab" data-tool-tab-value="${item}">${item}</button>`).join('')}</nav>`;
  return workspaceShell('broker', 'Exchange must remain attributable and governed', content, tabs);
}

function envoyWorkspace(state) {
  return workspaceShell('envoy', 'Delegation does not create unlimited Authority', `<section class="tool-filterbar"><select id="envoy-realm"><option>All Realms</option><option>Build Kiduna</option><option>Service Alliance</option></select><button data-prototype-action="Configure Envoy mandate">Configure Envoy</button></section><div class="envoy-grid"><article class="governance-card urgent"><span>Vote · closes tomorrow</span><h2>Contribution recognition</h2><p>Direct Source vote · Build Kiduna</p><small>Current state: not cast · 62% participation</small><button data-prototype-action="Review Contribution recognition vote">Review vote</button></article><article class="governance-card"><span>Decision market · 3 days</span><h2>Release sequencing</h2><p>Envoy may allocate up to 180 compute under a revocable mandate.</p><small>Spent 40 / 180 · threshold 60 per position</small><button data-prototype-action="Inspect Release sequencing market">Inspect market</button></article><article class="governance-card"><span>Delegation</span><h2>Governance Envoy</h2><p>Build Kiduna only · proposals and votes · expires Sep 30.</p><small>Spend limit 200 · approval required above 60 · revocable now</small><div><button data-prototype-action="Revise Governance Envoy mandate">Revise</button><button data-prototype-action="Revoke Governance Envoy mandate">Revoke</button></div></article></div><section class="history-panel"><h2>Governance history</h2><p>Every direct vote and delegated Action retains actor, mandate, decision, outcome, and receipt.</p></section>`);
}

function sentinelWorkspace(state) {
  const boundaries = state.sentinelBoundaries;
  return workspaceShell('sentinel', 'Coherence without a universal score', `<div class="sentinel-columns"><section><span>Coherence &amp; Vibes</span><h2>Reception tolerances</h2><p>These Source-relative ranges affect staging and warnings. They do not rewrite underlying material.</p>${[['participation','Participation'],['stewardship','Stewardship'],['belonging','Belonging'],['legibility','Authority legibility']].map(([id,label]) => `<label class="sentinel-range"><span>${label}<output>${boundaries[id]}</output></span><input type="range" min="0" max="100" value="${boundaries[id]}" data-sentinel-range="${id}"></label>`).join('')}</section><section><span>Content boundaries</span><h2>What reaches you, and how</h2><p>Adults may choose Open where legally and technically permitted.</p><div class="boundary-presets">${['Guided','Balanced','Open'].map(item => `<button class="${state.sentinelPreset === item ? 'active' : ''}" data-sentinel-preset="${item}">${item}</button>`).join('')}</div>${['Age appropriateness','Violence','Trauma','Sexual content','Racial identity and topics','Harassment or abuse','Graphic material','Distressing subjects'].map(item => `<label class="boundary-row"><span>${item}</span><select data-boundary-topic="${item}"><option>Warn</option><option>Hold</option><option>Allow</option></select></label>`).join('')}</section><section><span>Verification &amp; trust</span><h2>Evidence stays attributable</h2><div class="sentinel-list"><button data-prototype-action="Open identity verification">Identity verification <b>2 current</b></button><button data-prototype-action="Open evidence review">Evidence review <b>4 pending</b></button><button data-prototype-action="Open peer verification">Peer verification <b>Contextual</b></button><button data-prototype-action="Open disputes and corrections">Disputes &amp; corrections <b>1 open</b></button><button data-prototype-action="Open verification history">Verification history <b>Inspect</b></button></div></section><section><span>Security &amp; privacy</span><h2>Boundaries and reasons</h2><div class="sentinel-list"><button data-prototype-action="Open visibility rules">Visibility rules <b>Personal / Private / Secret / Public</b></button><button data-prototype-action="Review held Signals">Held Signals <b>3 · reasons visible</b></button><button data-prototype-action="Review security alerts">Security alerts <b>1 attention</b></button><button data-prototype-action="Review permissions and Consent">Permissions &amp; Consent <b>Review</b></button><button data-prototype-action="Review retention">Data retention <b>4 policies</b></button></div></section></div>`);
}

function connectorWorkspaceRefined(state) {
  const tab = state.connectorWorkspaceTab;
  const connection = id => state.connectorConnections[id] || { connected: false, active: false, muted: false };
  const tabs = `<nav class="tool-tabs" aria-label="Connector sections">${[['Agents','People & helpers'],['Systems','Tools & services'],['Realms','Places & groups']].map(([value,label]) => `<button class="${tab === value ? 'active' : ''}" type="button" data-tool-tab="connectorWorkspaceTab" data-tool-tab-value="${value}">${label}</button>`).join('')}</nav>`;
  const connections = [
    ['moto-avatar','M','Moto in Build Kiduna','Your public face for this working group','Can represent you here; cannot spend or vote'],
    ['research-ally','R','Research Ally','A private thinking and research companion','Can draft and advise; you approve anything outward'],
    ['budget-actor','B','Budget Helper','Keeps an eye on the weekly project budget','Can record receipts within a $200 weekly limit'],
    ['alice-avatar','A','Alice','A direct relationship with another person','Can message and share what both of you allow']
  ];
  const systems = [
    ['google','G','Google Workspace','Calendar, Meet, Gmail, and YouTube','Calendar: read · Gmail: off'],
    ['bluesky','B','Bluesky','Public posts and conversations','Posting requires approval'],
    ['telegram','T','Telegram','Messages and groups','Two groups connected'],
    ['solana','S','Solana','Wallet and payment activity','Read balances · sending off']
  ];
  const places = [
    ['B','Build Kiduna','Member · active','Building tools that expand human agency','3 open conversations'],
    ['S','Service Alliance','Guest · invited','Helping people find care, service, and useful work','Invitation waiting'],
    ['K','Kiduna Commons','Not joined','Shared knowledge, culture, and public experiments','Open to explore']
  ];
  let content = '';
  if (tab === 'Agents') content = `<section class="workspace-intro"><div><span>Your trusted circle</span><h2>Who can show up with you?</h2><p>Each relationship has its own permissions. Connecting someone never gives them unlimited control.</p></div><button data-prototype-action="Find someone to connect">Find a connection</button></section><div class="connection-grid">${connections.map(([id,initial,name,relationship,permission]) => { const status = connection(id); return `<article class="connection-card"><div class="connection-person"><i>${initial}</i><div><span>${status.connected ? status.active ? 'Connected · active' : 'Connected · paused' : 'Not connected'}</span><h3>${name}</h3><p>${relationship}</p></div></div><div class="permission-note"><small>What this allows</small><strong>${permission}</strong></div><footer><button data-connection-action="${status.connected ? 'disconnect' : 'connect'}" data-connection-id="${id}">${status.connected ? 'Disconnect' : 'Connect'}</button><button class="primary" data-connection-action="${status.active ? 'deactivate' : 'activate'}" data-connection-id="${id}" ${status.connected ? '' : 'disabled'}>${status.active ? 'Pause' : 'Activate'}</button><button data-prototype-action="Review ${name} permissions">Permissions</button></footer></article>`; }).join('')}</div>`;
  if (tab === 'Systems') content = `<section class="workspace-intro"><div><span>Your tools</span><h2>Bring the useful parts with you.</h2><p>Choose the exact service and access you need. Nothing here creates a live connection in this prototype.</p></div><button data-prototype-action="Connect another tool">Connect a tool</button></section><div class="connection-grid">${systems.map(([id,initial,name,description,permission]) => { const status = connection(id); return `<article class="connection-card"><div class="connection-person"><i>${initial}</i><div><span>${status.connected ? 'Prototype connected' : 'Not connected'}</span><h3>${name}</h3><p>${description}</p></div></div><div class="permission-note"><small>Current access</small><strong>${permission}</strong></div><footer><button data-connection-action="${status.connected ? 'disconnect' : 'connect'}" data-connection-id="${id}">${status.connected ? 'Disconnect' : 'Connect'}</button><button class="primary" data-prototype-action="Choose ${name} access">Choose access</button><button data-prototype-action="See ${name} activity">Activity</button></footer></article>`; }).join('')}</div>`;
  if (tab === 'Realms') content = `<section class="workspace-intro"><div><span>Places you belong</span><h2>Stay close to the groups that matter.</h2><p>Membership, invitations, roles, and decision power are always shown separately.</p></div><button data-prototype-action="Find a place or group">Find a place</button></section><div class="connection-grid">${places.map(([initial,name,status,purpose,next]) => `<article class="connection-card"><div class="connection-person"><i>${initial}</i><div><span>${status}</span><h3>${name}</h3><p>${purpose}</p></div></div><div class="permission-note"><small>Waiting for you</small><strong>${next}</strong></div><footer><button class="primary" data-prototype-action="Open ${name}">Open</button><button data-prototype-action="Review your relationship with ${name}">Your access</button><button data-prototype-action="Invite someone to ${name}">Invite</button></footer></article>`).join('')}</div><aside class="workspace-callout"><div><span>Invitation links</span><h3>Give someone a clear way in.</h3><p>Create a welcome link with a destination, expiration, audience, and the access being offered.</p></div><button data-prototype-action="Create an invitation link">Create link</button></aside>`;
  return workspaceShell('connector', 'Your connections, on your terms', content, tabs);
}

function creatorWorkspaceRefined(state) {
  const groups = [['Ideas & meaning',['Maps','Places','Nodes','Media']],['People & helpers',['Avatars','Allies','Actors','Automations']],['Value & exchange',['Offers','Resources','Actions','Movements']],['Experiences',['Composed','Setting','Pulse','Graph','Theater','World','Reader','Game']]];
  const layouts = { composed:'A familiar interface with useful connections between its parts.', setting:'A room or place people can move through together.', pulse:'A living view of many things changing at once.', graph:'Relationships you can see, follow, and explore.', theater:'A focused stage for watching and listening.', world:'An immersive place for presence, movement, and discovery.', reader:'A calm flow for reading, watching, or listening.', game:'Rules, choices, challenge, feedback, and play.' };
  return workspaceShell('creator', 'Start with what you want to make possible', `<section class="workspace-intro"><div><span>Make something</span><h2>Give the idea a shape.</h2><p>Start small, invite collaborators when you are ready, and preview everything before other people see it.</p></div><button data-prototype-action="Continue your last draft">Continue last draft</button></section><div class="creator-groups">${groups.map(([group,items]) => `<section><span>${group}</span><div>${items.map(item => `<button type="button" data-create-kind="${item}">${item}</button>`).join('')}</div></section>`).join('')}</div><section class="creator-flow"><div><span>New draft</span><h2>What do you want to bring into the world?</h2><p>You can change the shape, privacy, and collaborators before sharing it.</p></div><div class="creator-fields"><label>Name<input id="creator-name" value="${escapeHtml(state.creatorDraftName)}" placeholder="Give it a memorable name"></label><label>Purpose<textarea id="creator-purpose" rows="3" placeholder="What should change because this exists?">${escapeHtml(state.creatorDraftPurpose)}</textarea></label><label>Who can see it?<select><option>Only me</option><option>People I choose</option><option>A group I belong to</option><option>Anyone</option></select></label><label>Who is making it with you?<input placeholder="Invite people or helpers…"></label></div><div class="layout-example-grid">${layoutTypes.map(type => `<button class="layout-example ${state.creatorLayout === type.id ? 'selected' : ''}" type="button" data-creator-layout="${type.id}"><span>${type.name}</span><b>${escapeHtml(type.meta)}</b><p>${escapeHtml(layouts[type.id])}</p></button>`).join('')}</div><div class="creator-stage-actions"><button data-prototype-action="Preview your draft">Preview</button><button data-prototype-action="Invite collaborators to your draft">Invite collaborators</button><button class="primary" data-prototype-action="Get your draft ready to share">Get ready to share</button></div></section>`);
}

function inspectorWorkspaceRefined(state) {
  const records = [
    ['Invitation','Alice invited you to Service Alliance','Today · 10:42 AM','Delivered privately; you have not responded yet.'],
    ['Change','Layout Site went live','Yesterday · 4:18 PM','Published by Moto after review; the previous version remains in history.'],
    ['Payment','$240 design review payment','Aug 31 · 2:06 PM','Paid by Build Kiduna and received by Moto. Receipt verified.'],
    ['Opened','You opened Systems Oracle','Aug 30 · 9:14 AM','Viewing it did not change the original or share anything outward.'],
    ['Correction','Your Build Kiduna role was corrected','Aug 29 · 5:23 PM','The old label remains in history with a note explaining the change.']
  ];
  const query = state.inspectorWorkspaceQuery.toLowerCase();
  return workspaceShell('inspector-workspace', 'The story behind what you see', `<section class="workspace-intro"><div><span>History & receipts</span><h2>Look closer without losing the thread.</h2><p>Find the source, decisions, changes, payments, and boundaries behind anything you can see.</p></div><button data-prototype-action="Scan a link or receipt">Scan something</button></section><section class="tool-filterbar"><label>${icon('search',16)}<input id="inspector-workspace-search" value="${escapeHtml(state.inspectorWorkspaceQuery)}" placeholder="Search people, places, payments, or changes…"></label><select><option>Everything</option><option>Invitations</option><option>Changes</option><option>Payments</option><option>Things opened</option></select><select><option>Any time</option><option>Today</option><option>Last 7 days</option><option>Choose dates</option></select></section><div class="inspector-ledger refined-ledger">${records.filter(row => !query || row.join(' ').toLowerCase().includes(query)).map(([type,title,time,detail],index) => `<article><i>${index + 1}</i><div><span>${type} · ${time}</span><h2>${title}</h2><p>${detail}</p></div><div><button data-prototype-action="See the full story behind ${title}">Full story</button><button data-prototype-action="See who was involved in ${title}">Who was involved</button><button data-prototype-action="See access details for ${title}">Access</button></div></article>`).join('')}</div>`);
}

function brokerWorkspaceRefined(state) {
  const tab = state.brokerTab;
  const tabs = `<nav class="tool-tabs" aria-label="Broker sections">${['Overview','Sponsorships','Offers','Treasuries','History'].map(item => `<button class="${tab === item ? 'active' : ''}" data-tool-tab="brokerTab" data-tool-tab-value="${item}">${item}</button>`).join('')}</nav>`;
  const overview = `<div class="broker-metrics refined-metrics"><article><span>Available to you</span><strong>$8,420</strong><small>Across money, credits, and earned rewards</small></article><article><span>Earned this month</span><strong>$3,180</strong><small>From 7 completed contributions</small></article><article><span>Shared treasuries</span><strong>$27.6K</strong><small>Across 3 groups you belong to</small></article><article><span>Needs your attention</span><strong>2</strong><small>One approval · one expiring offer</small></article></div><div class="broker-layout"><section class="workspace-panel"><header><div><span>Recent activity</span><h2>Value moving through your work</h2></div><button data-prototype-action="Open all value activity">See all</button></header>${[['+$240','Design review completed','Build Kiduna · received yesterday'],['−$85','Research materials','Approved expense · yesterday'],['+$1,200','Welcome Path milestone','Service Alliance · Aug 30'],['+$46','Royals & Rogues sales','14 purchases · Aug 29']].map(([amount,title,detail]) => `<article class="value-row"><strong>${amount}</strong><div><b>${title}</b><small>${detail}</small></div><button data-prototype-action="Inspect ${title}">Details</button></article>`).join('')}</section><section class="workspace-panel"><header><div><span>Open opportunities</span><h2>Ways to contribute and earn</h2></div></header>${[['$600–$900','Facilitate two community sessions','Care Within Reach'],['$240','Review a new interface','Tools People Can Trust'],['$120','Document a local watershed walk','Living Watersheds']].map(([amount,title,group]) => `<article class="opportunity-row"><span>${amount}</span><h3>${title}</h3><small>${group}</small><button data-prototype-action="Open opportunity: ${title}">Take a look</button></article>`).join('')}</section></div>`;
  const sponsorships = `<section class="workspace-intro"><div><span>Purpose-backed support</span><h2>Help good work go farther.</h2><p>See exactly what support enables, what it does not buy, and how progress will be shared.</p></div><button data-prototype-action="Offer support">Offer support</button></section><div class="connection-grid"><article class="connection-card"><span>Awaiting group approval</span><h3>Welcome Path rehearsals</h3><p>$500 for three hosted invitation sessions and participant care.</p><div class="permission-note"><small>Important boundary</small><strong>Support does not buy editorial or governance control.</strong></div><footer><button data-prototype-action="Review Welcome Path support">Review details</button><button class="primary" data-prototype-action="Approve Welcome Path support">Approve</button></footer></article><article class="connection-card"><span>Ready to accept</span><h3>Research capacity gift</h3><p>$100 for evidence gathering, offered without a reciprocal requirement.</p><div class="permission-note"><small>Recipient</small><strong>Research Ally · personal workspace</strong></div><footer><button data-prototype-action="Review research gift">Review details</button><button class="primary" data-prototype-action="Accept research gift">Accept</button></footer></article></div>`;
  const general = `<section class="workspace-intro"><div><span>${tab}</span><h2>${tab === 'Offers' ? 'Clear terms before anyone commits.' : tab === 'Treasuries' ? 'Shared resources, visible stewardship.' : 'A complete trail of value and work.'}</h2><p>${tab === 'Offers' ? 'Compare open work, compensation, timing, and expectations in one place.' : tab === 'Treasuries' ? 'See balances, purposes, recent payments, and decisions waiting for approval.' : 'Follow earnings, payments, gifts, spending, and corrections over time.'}</p></div><button data-prototype-action="Open ${tab.toLowerCase()} filters">Filter</button></section>${overview}`;
  return workspaceShell('broker', 'Work, resources, and fair exchange', tab === 'Overview' ? overview : tab === 'Sponsorships' ? sponsorships : general, tabs);
}

function envoyWorkspaceRefined() {
  return workspaceShell('envoy', 'Your voice, with clear boundaries', `<section class="workspace-intro"><div><span>Decisions</span><h2>Choose when to vote—and when to delegate.</h2><p>You can always see who decided, what they were allowed to do, and how to take that permission back.</p></div><button class="primary" data-prototype-action="Set up a trusted delegate">Set up a delegate</button></section><div class="envoy-grid refined-envoy"><article class="governance-card urgent"><span>Your vote · closes tomorrow</span><h2>How should contributions be recognized?</h2><p>Build Kiduna is choosing a fairer way to credit visible and behind-the-scenes work.</p><div class="decision-progress"><i style="width:62%"></i></div><small>62% have taken part · you have not voted</small><button class="primary" data-prototype-action="Review contribution recognition vote">Review and vote</button></article><article class="governance-card"><span>Group decision · 3 days left</span><h2>What should ship next?</h2><p>Your delegate may place up to 60 credits on a priority, within the limit you set.</p><div class="decision-progress"><i style="width:42%"></i></div><small>40 of 180 credits used · change or revoke anytime</small><button data-prototype-action="Review release priorities">See the decision</button></article><article class="governance-card"><span>Your active delegation</span><h2>Governance Helper</h2><p>Can review proposals and vote in Build Kiduna through September 30.</p><div class="mandate-list"><small>Cannot spend more than $60 without you</small><small>Cannot change membership or privacy</small><small>Every decision appears in your history</small></div><div><button data-prototype-action="Change Governance Helper limits">Change limits</button><button data-prototype-action="Revoke Governance Helper">Revoke</button></div></article></div><section class="workspace-panel governance-history"><header><div><span>Recent decisions</span><h2>A trail you can understand</h2></div><button data-prototype-action="Open all decision history">See all</button></header>${[['You voted','Keep the community fund open','Yesterday'],['Governance Helper voted','Approve the August release budget','Aug 29'],['You changed a limit','Maximum payment lowered to $60','Aug 25']].map(([actor,decision,time]) => `<article class="value-row"><strong>✓</strong><div><b>${decision}</b><small>${actor} · ${time}</small></div><button data-prototype-action="Inspect decision: ${decision}">Details</button></article>`).join('')}</section>`);
}

function sentinelWorkspaceRefined(state) {
  const boundaries = state.sentinelBoundaries;
  return workspaceShell('sentinel', 'Make the Field feel right for you', `<section class="workspace-intro"><div><span>Comfort, trust & privacy</span><h2>Set your boundaries once. Change them anytime.</h2><p>These choices shape what reaches you and when you get a warning. They never rewrite the original.</p></div><button data-prototype-action="Review what is currently held">Review 3 held items</button></section><div class="sentinel-columns"><section><span>What feels aligned</span><h2>Your reception range</h2><p>Move each range to tune what arrives naturally and what stays at the edge.</p>${[['participation','Active participation'],['stewardship','Care for shared resources'],['belonging','Belonging and welcome'],['legibility','Clear people and permissions']].map(([id,label]) => `<label class="sentinel-range"><span>${label}<output>${boundaries[id]}%</output></span><input type="range" min="0" max="100" value="${boundaries[id]}" data-sentinel-range="${id}"></label>`).join('')}</section><section><span>Content comfort</span><h2>How should sensitive material arrive?</h2><p>Choose a starting point, then adjust individual topics below.</p><div class="boundary-presets">${['Guided','Balanced','Open'].map(item => `<button class="${state.sentinelPreset === item ? 'active' : ''}" data-sentinel-preset="${item}">${item}</button>`).join('')}</div>${['Age appropriateness','Violence','Trauma','Sexual content','Racial identity and topics','Harassment or abuse','Graphic material','Distressing subjects'].map(item => `<label class="boundary-row"><span>${item}</span><select data-boundary-topic="${item}"><option>Warn me</option><option>Hold it</option><option>Let it through</option></select></label>`).join('')}</section><section><span>Trust & evidence</span><h2>Know what you can rely on</h2><div class="sentinel-list"><button data-prototype-action="Open identity checks">Identity checks <b>2 current</b></button><button data-prototype-action="Open evidence review">Evidence to review <b>4 waiting</b></button><button data-prototype-action="Open peer context">What trusted peers say <b>Available</b></button><button data-prototype-action="Open corrections">Corrections and disputes <b>1 open</b></button><button data-prototype-action="Open trust history">Your trust history <b>View</b></button></div></section><section><span>Privacy & security</span><h2>Who can see what—and why?</h2><div class="sentinel-list"><button data-prototype-action="Open visibility choices">Visibility choices <b>Review</b></button><button data-prototype-action="Review held items">Held for you <b>3 · reasons shown</b></button><button data-prototype-action="Review security alerts">Security alerts <b>1 needs attention</b></button><button data-prototype-action="Review permissions">Permissions and consent <b>Review</b></button><button data-prototype-action="Review retention choices">How long things are kept <b>4 choices</b></button></div></section></div>`);
}

function metricsWorkspace(state) {
  const active = purposeScopes[state.metricsScope] || purposeScopes.network;
  const purposeOptions = Object.entries(purposeScopes).map(([id, scope]) => `<option value="${id}" ${id === state.metricsScope ? 'selected' : ''}>${escapeHtml(scope.name)}</option>`).join('');
  const movements = Object.entries(purposeScopes).filter(([id]) => id !== 'network');
  return workspaceShell('metrics', 'Purpose in motion', `<section class="metrics-top" style="--purpose-color:${active.color}"><div class="metrics-purpose"><span>Prototype network view</span><label for="metrics-scope">See what is happening in</label><select id="metrics-scope">${purposeOptions}</select><h2>${escapeHtml(active.purpose)}</h2><p>Numbers matter when they help people see whether participation, resources, and livelihoods are actually growing around a shared purpose.</p></div><div class="metrics-scoreboard"><article><small>People taking part</small><strong>${active.people}</strong><em>${active.growth} this year</em></article><article><small>People contributing now</small><strong>${active.contributors}</strong><em>Paid and unpaid work</em></article><article><small>Value moving</small><strong>${active.volume}</strong><em>Last 30 days · USD</em></article><article><small>Paid to contributors</small><strong>${active.payments}</strong><em>Work, rewards, and fees</em></article><article><small>Shared resources</small><strong>${active.treasury}</strong><em>Treasuries and committed funds</em></article><article><small>Participation growth</small><strong>${active.growth}</strong><em>Compared with last year</em></article></div></section><div class="metric-panels"><section class="metric-chart-card"><header><div><span>Participation</span><h2>More people are staying involved.</h2></div><select aria-label="Participation range"><option>Last 12 months</option><option>Last 90 days</option><option>Last 30 days</option></select></header><div class="participation-chart" aria-label="Prototype participation chart"><div style="--h:34%"><i></i><small>Oct</small></div><div style="--h:42%"><i></i><small>Nov</small></div><div style="--h:40%"><i></i><small>Dec</small></div><div style="--h:51%"><i></i><small>Jan</small></div><div style="--h:58%"><i></i><small>Feb</small></div><div style="--h:61%"><i></i><small>Mar</small></div><div style="--h:69%"><i></i><small>Apr</small></div><div style="--h:76%"><i></i><small>May</small></div><div style="--h:73%"><i></i><small>Jun</small></div><div style="--h:82%"><i></i><small>Jul</small></div><div style="--h:88%"><i></i><small>Aug</small></div><div style="--h:94%"><i></i><small>Sep</small></div></div><div class="chart-legend"><span><i class="new"></i>New participants</span><span><i class="returning"></i>Returning</span><span><i class="earning"></i>Earning through the work</span></div></section><section class="metric-chart-card"><header><div><span>Where value went</span><h2>People received the largest share.</h2></div><small>Last 30 days</small></header><div class="flow-stack"><div style="--w:42%;--c:#a876ff"><span>Payments to people</span><strong>42%</strong></div><div style="--w:24%;--c:#68d8ff"><span>Partner and vendor work</span><strong>24%</strong></div><div style="--w:19%;--c:#56d7ae"><span>Grants and rewards</span><strong>19%</strong></div><div style="--w:15%;--c:#ffc95c"><span>Treasury growth</span><strong>15%</strong></div></div><p class="metric-insight">For every $1 held in shared treasuries, $1.35 moved to people doing the work this month.</p></section></div><section class="movement-table"><header><div><span>Movements</span><h2>Different purposes. One living network.</h2><p>Choose one above for a closer view, or compare where people, work, and resources are gathering.</p></div><button data-prototype-action="Explore every movement">Explore all</button></header><div class="movement-head"><span>Purpose</span><span>People</span><span>Contributors</span><span>Volume</span><span>Paid for work</span><span>Momentum</span></div>${movements.map(([id,scope]) => `<button class="movement-row ${id === state.metricsScope ? 'active' : ''}" type="button" data-metrics-scope="${id}"><span><i style="background:${scope.color}"></i><b>${escapeHtml(scope.name)}</b><small>${escapeHtml(scope.purpose)}</small></span><strong>${scope.people}</strong><strong>${scope.contributors}</strong><strong>${scope.volume}</strong><strong>${scope.payments}</strong><em>${scope.growth}</em></button>`).join('')}</section>`);
}

export function workspaceFor(state) {
  if (state.openPanel === 'connector') return connectorWorkspaceRefined(state);
  if (state.openPanel === 'creator') return creatorWorkspaceRefined(state);
  if (state.openPanel === 'inspector-workspace') return inspectorWorkspaceRefined(state);
  if (state.openPanel === 'broker') return brokerWorkspaceRefined(state);
  if (state.openPanel === 'envoy') return envoyWorkspaceRefined(state);
  if (state.openPanel === 'sentinel') return sentinelWorkspaceRefined(state);
  if (state.openPanel === 'metrics') return metricsWorkspace(state);
  return '';
}

export function expressionOverlay(state) {
  const item = expressions.find(expression => expression.id === state.activeExpressionId);
  if (!item) return '';
  return `<header class="active-expression-bar"><button type="button" data-action="close-expression">← Back to the Field</button><div><span>${layoutTypes.find(type => type.id === item.layout)?.name} view</span><strong>${escapeHtml(item.title)}</strong><small>${escapeHtml(item.realm)} · ${escapeHtml(item.privacy)}</small></div><button type="button" data-expression-inspect="${item.id}">About this</button></header>`;
}

export function expressionInspector(state) {
  const item = expressions.find(expression => expression.id === state.expressionInspectId);
  if (!item) return '';
  return `<aside class="expression-inspector" role="dialog" aria-label="About ${escapeHtml(item.title)}"><header><div><span>About this</span><h2>${escapeHtml(item.title)}</h2></div><button type="button" data-action="close-expression-inspector" aria-label="Close details">${icon('close',18)}</button></header><p>${escapeHtml(item.description)}</p><dl><div><dt>Format</dt><dd>${layoutTypes.find(type => type.id === item.layout)?.name}</dd></div><div><dt>Place</dt><dd>${escapeHtml(item.realm)}</dd></div><div><dt>Shared by</dt><dd>${escapeHtml(item.source)}</dd></div><div><dt>Who can see it</dt><dd>${escapeHtml(item.privacy)} · ${escapeHtml(item.access)}</dd></div><div><dt>What is inside</dt><dd>${item.parts.map(escapeHtml).join(' · ')}</dd></div><div><dt>Why you are seeing it</dt><dd>${escapeHtml(item.why)}</dd></div><div><dt>History</dt><dd>Its parts keep their own source and change history.</dd></div></dl><small>Opening this changes your view only. It does not share, approve, or alter the original.</small><button class="primary" type="button" data-expression-open="${item.id}">Open</button></aside>`;
}

function actionDetail(action) {
  return `<small><b>Target:</b> ${escapeHtml(action.target)} · <b>Scope:</b> ${escapeHtml(action.scope)} · <b>Authority:</b> ${escapeHtml(action.authority)} · <b>Privacy:</b> ${escapeHtml(action.privacy)} · <b>Reversibility:</b> ${escapeHtml(action.reversibility)}</small>`;
}

function legacyUniversalKi(state) {
  const proposal = state.kiProposal;
  return `<section class="receiver-v2-ki universal-ki" aria-label="Work with Ki"><div class="ki-word-circle" aria-hidden="true">Ki</div><label><span>Ki · ${state.routeLabel}</span><input id="universal-ki-input" value="${escapeHtml(state.kiDraft)}" placeholder="Ask Ki to find something, explain it, or help you take the next step…"></label><div class="receiver-v2-voice-actions"><button type="button" data-action="universal-dictate"><span class="interface-icon icon-microphone" aria-hidden="true"></span><span>Dictate</span></button><button type="button" data-action="universal-voice"><span class="interface-icon icon-voice-live" aria-hidden="true"></span><span>Voice</span></button><button class="receiver-v2-preview universal-preview" type="button" data-action="ki-preview">Preview actions</button></div>${proposal ? `<aside class="ki-action-review" role="dialog" aria-label="Review Ki’s proposed actions"><header><div><span>Review Ki’s proposed actions</span><h2>Nothing happens until you approve.</h2></div><button type="button" data-action="ki-cancel" aria-label="Close action review">${icon('close',18)}</button></header>${state.kiRevisionMode ? `<section class="ki-revise"><p>Tell Ki what to keep, remove, narrow, or change. The revised proposal will return for review and will not be applied.</p><textarea id="ki-revision-input" rows="3" placeholder="Revise the proposal…">${escapeHtml(state.kiRevisionDraft)}</textarea><div><button type="button" data-action="ki-revision-cancel">Back</button><button class="primary" type="button" data-action="ki-revision-preview">Preview revision</button></div></section>` : `<div class="ki-review-toolbar"><button type="button" data-action="ki-select-all">Select all</button><button type="button" data-action="ki-clear-all">Clear all</button><span>${state.kiSelectedActions.length} of ${proposal.actions.length} selected</span></div><div class="ki-action-list">${proposal.actions.map(action => `<label><input type="checkbox" data-ki-action-check="${action.id}" ${state.kiSelectedActions.includes(action.id) ? 'checked' : ''}><span><b>${escapeHtml(action.title)}</b><em>${escapeHtml(action.effect)}</em>${actionDetail(action)}<i>${action.classification}</i></span></label>`).join('')}</div><footer><button type="button" data-action="ki-revise">Revise</button><button type="button" data-action="ki-cancel">Cancel</button><button type="button" data-action="ki-approve-selected" ${state.kiSelectedActions.length ? '' : 'disabled'}>Approve selected</button><button class="primary" type="button" data-action="ki-approve-all">Approve all</button></footer>`}</aside>` : ''}</section>`;
}

export function universalKi(state) {
  const proposal = state.kiProposal;
  const additions = [
    ['Attach a file', 'Attach a file to this Ki request'],
    ['Add a link', 'Add a link to this Ki request'],
    ['Bring in something from Receiver', 'Bring in something from Receiver'],
    ['Reference a Field item', 'Reference a Field item']
  ];
  return `<section class="receiver-v2-ki universal-ki" id="ki" aria-label="Work with Ki">
    <div class="ki-plus-wrap">
      <button class="ki-plus-button" type="button" data-action="open-ki-plus" aria-label="Add context to Ki" aria-expanded="${state.kiPlusOpen ? 'true' : 'false'}" aria-controls="ki-plus-menu">+</button>
      ${state.kiPlusOpen ? `<div class="ki-plus-menu" id="ki-plus-menu" role="menu"><header><strong>Add context</strong><button type="button" data-action="close-ki-plus" aria-label="Close add-context menu">${icon('close', 15)}</button></header>${additions.map(([label, value]) => `<button type="button" role="menuitem" data-action="ki-plus-option" data-ki-plus-value="${escapeHtml(value)}">${escapeHtml(label)}</button>`).join('')}<small>Prototype only. Choosing an option stages a request; it does not attach or share anything.</small></div>` : ''}
    </div>
    <div class="ki-word-circle" aria-hidden="true">Ki</div>
    <label><span>Ki · ${escapeHtml(state.routeLabel)}</span><input id="universal-ki-input" value="${escapeHtml(state.kiDraft)}" placeholder="Ask Ki to find something, explain it, or help you take the next step…"></label>
    <div class="receiver-v2-voice-actions"><button type="button" data-action="universal-dictate"><span class="interface-icon icon-microphone" aria-hidden="true"></span><span>Dictate</span></button><button type="button" data-action="universal-voice"><span class="interface-icon icon-voice-live" aria-hidden="true"></span><span>Voice</span></button><button class="receiver-v2-preview universal-preview" type="button" data-action="ki-preview">Preview actions</button></div>
    ${proposal ? `<aside class="ki-action-review" role="dialog" aria-label="Review Ki’s proposed actions"><header><div><span>Review Ki’s proposed actions</span><h2>Nothing happens until you approve.</h2></div><button type="button" data-action="ki-cancel" aria-label="Close action review">${icon('close',18)}</button></header>${state.kiRevisionMode ? `<section class="ki-revise"><p>Tell Ki what to keep, remove, narrow, or change. The revised proposal will return for review and will not be applied.</p><textarea id="ki-revision-input" rows="3" placeholder="Revise the proposal…">${escapeHtml(state.kiRevisionDraft)}</textarea><div><button type="button" data-action="ki-revision-cancel">Back</button><button class="primary" type="button" data-action="ki-revision-preview">Preview revision</button></div></section>` : `<div class="ki-review-toolbar"><button type="button" data-action="ki-select-all">Select all</button><button type="button" data-action="ki-clear-all">Clear all</button><span>${state.kiSelectedActions.length} of ${proposal.actions.length} selected</span></div><div class="ki-action-list">${proposal.actions.map(action => `<label><input type="checkbox" data-ki-action-check="${action.id}" ${state.kiSelectedActions.includes(action.id) ? 'checked' : ''}><span><b>${escapeHtml(action.title)}</b><em>${escapeHtml(action.effect)}</em>${actionDetail(action)}<i>${action.classification}</i></span></label>`).join('')}</div><footer><button type="button" data-action="ki-revise">Revise</button><button type="button" data-action="ki-cancel">Cancel</button><button type="button" data-action="ki-approve-selected" ${state.kiSelectedActions.length ? '' : 'disabled'}>Approve selected</button><button class="primary" type="button" data-action="ki-approve-all">Approve all</button></footer>`}</aside>` : ''}
  </section>`;
}

export function proposeKiActions(state, revision = '') {
  const context = state.routeLabel;
  const text = (revision || state.kiDraft || '').trim();
  const base = [
    { id:'focus', title:`Refine the ${context} view`, effect:text ? `Use “${text}” to adjust the current local presentation.` : 'Bring the most relevant local material into the current view.', target:context, scope:'Current browser view', authority:'Moto · local presentation', privacy:'No visibility widening', reversibility:'Fully reversible', classification:'LOCAL · PRESENTATION' },
    { id:'inspect', title:'Open supporting detail', effect:'Stage provenance, boundaries, and reasons beside the selected material.', target:'Current selection', scope:'Inspector overlay', authority:'Read-only authorized view', privacy:'Existing access only', reversibility:'Close overlay', classification:'LOCAL · READ ONLY' },
    { id:'save', title:'Save this working arrangement', effect:'Persist the current filters and arrangement in this browser.', target:'Local Layout state', scope:'This device', authority:'Moto', privacy:'Local storage', reversibility:'Reset or change later', classification:'LOCAL · STATE CHANGE' }
  ];
  return { prompt: text, revision, actions: base };
}
